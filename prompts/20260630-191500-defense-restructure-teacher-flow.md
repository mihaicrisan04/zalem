[Restructure to the teacher's required academic flow. The user first asked for 8 specific tweaks (remove title icon → "Author"; remove top kicker then instead add badges "problem / system architecture / feature · advisor / feature · context / feature · reviews / evaluation"; clarify where the classical engine fits in the flow; chat video → AirPods Pro 2 + MacBook Air, card like the real store card, remove the blue dot from "Shopping advisor"; grill notes + code locations for the context flow and the review/embedding pipeline; clearer evals without the "twist"; minimal Thank-you slide), then pivoted with the supervisor's Romanian email prescribing the defense structure:]

Teacher's structure (paraphrased from the RO email): slide 1 title + name; slide 2 table of contents (chapters / key subchapters); then theoretical content starting with the domain and relevant state-of-the-art results from the literature; then the student's idea and approach with the relevant functionalities; then results and conclusions; THEN a LIVE app demo (not a recording), with accounts pre-connected and servers warmed up. Airy slides, high contrast, don't read from notes. Be ready for committee questions drawn straight from the thesis text AND the code (show source for a feature; SOTA comparison; design patterns; integrating other data sources; input validation; testing; invalid data; hardest part; and AI-specific: parameters / overfitting / hidden layers / false-positives-negatives).

User instruction: study the actual thesis content + our approach deeply with multiple agents, form a strong research-backed plan, then modify the presentation and show it for iteration. Keep "less is more". A 1–2 min live demo video will be added by the user at the end.

---

Ran a 9-agent research workflow over the thesis chapters + codebase (saved to `presentation/research/`: defense-plan.md, qa-prep.md, research-dossier.md). Key findings surfaced and confirmed with the user: (1) the classical engine and the AI advisor are TWO INDEPENDENT paths — classical powers the store rails (home/product/cart) and never calls the LLM; the advisor reads the catalog directly; the only link is the optional `getRecommendations` tool; (2) the thesis prose oversells the ranking (RRF "k=60", "two-stage LLM rerank", switching thresholds) vs the code (additive scoring + classical MMR) — grounding claims are fully code-backed. User chose: honest two-lane architecture + Q&A prep; ALL THREE functionalities (advisor, context, reviews); KEEP BOTH concept videos. Rebuilt `index.html` to 12 academic slides (title → contents → domain → Rufus SOTA → idea+two-lane architecture diagram → 3 feature slides → ablation → sweep → conclusions → thank-you) with top badges, clarified eval metrics (defined "specific claim", moved the AI-judge twist to speaker notes). Updated both Remotion videos to real products (AirPods Pro 2 / MacBook Air), store-card styling, removed the chat blue dot, fixed the context caption to "session state · rebuilt on every question"; re-rendered transparent webm + posters. Updated script.md (12-slide notes + demo plan) and README; regenerated the 12-page PDF and verified every slide.
