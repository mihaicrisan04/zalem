go ahead, u have access to run these commands from the backend dir

---

phase 8.3 done: dumped real product IDs from the seeded dev DB across smartphones / laptops / tablets / beauty; grew shopping-v1.yaml to 25 rows with real productIds + recentlyViewedIds + expectedTools (multi_turn deferred to v2, slots reallocated); wired groundedness.ts to live `api.products.get` via anyApi + self-typed FunctionReference; wired reviewThemeFidelity.ts to a new `ai/evals/checkThemes.ts` action that embeds claimed themes via embedMany and scores cosine similarity ≥ 0.35 against ≥ 2 reviews (with `ai/evals/checkThemesHelpers.ts` internal query for the embedded reviews); replaced built-in tool-call-f1 with custom expectedToolCoverage.ts that reads our own metadata.toolCalls shape; fixed Promptfoo single-element-array→string normalisation bug; brought runOnce.ts into parity with advisor.ts by adding the review-summary fetch; fixed bug where Promptfoo injects a basePath field into the provider config that Convex rejects (whitelisted clean fields in advisorProvider). first real sweep: 25 rows × 1 config = 34 row-runs, 11/34 fully passing. Eval surfaced its first real prompt-design finding: agent's responses on simple queries reference products from the few-shot examples (e.g. "specific keyboard" when asked about an iPhone 6) — measurable evidence the few-shots contaminate ambiguous queries.
