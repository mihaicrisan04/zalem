alright i read the plan and i approve, go ahead and start with 8.2

---

implemented phase 8.2: created `packages/backend/convex/ai/evals/runOnce.ts` (eval-only Convex action with env-secret auth, fresh per-call Agent for per-run model/prompt/maxSteps, dbSnapshot pre+post run, returns full result blob); split out `evalModel.ts` (per-run OpenRouter builder) and `promptVariants.ts` (current/be-efficient/no-fewshot resolver); wired `packages/eval/src/providers/advisorProvider.ts` to the new action via convex `anyApi` + self-typed FunctionReference (avoids leaking backend tsconfig strictness errors into eval); added `evaluateOptions.maxConcurrency: 4` to promptfoo config to respect Cerebras rate limits; fixed `import.meta.dir` → Node URL pattern; `bun run check-types` passes clean.
