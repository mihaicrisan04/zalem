alright go ahead and make a full plan for implmenting this phase. read all the required skills. if u think u can use it read it. better have the context rather than not. absolutely reserach how cron jobs work on convex and especially resreach for anything backend related, make sure phases are complete and thoroughly done, also i want u to continue documenting this journey in the docs dir. u can create there separate files or dirs where we write how this application is getting build what errors we encoutner evetything. this is an agentic development journey

---

loaded convex-cron-jobs, convex-functions, convex-best-practices skills. read recommendations-plan.md thoroughly. explored current backend state (schema, queries, seed data structure). designed and implemented phase 3: static recommendation engine with 6 algorithms (content similarity, Jaccard co-occurrence, exponential decay trending, switching hybrid personalization, cart recommendations, MMR diversity). created recommendationHelpers.ts (pure TS, no Convex imports), recommendations.ts (5 queries + internal recomputation functions + initialization action), crons.ts (daily jobs), wired into all UI slots. created docs/dev-journal/ for ongoing documentation.
