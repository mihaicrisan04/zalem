i want you now to reserach which of convexes components we can use to make our life and implmentation way easier and elegant. reserach how compoents work.
also does having this project as a monorepo helps in any way. can we levarge/ should we levarge other technologies that might be better suited in terms of performance and create a separate service and communicate via queues or api? think about this and reserach based on the plans we have what is the optimal approach. i target more an elegant system rather than squeezing every bit of performance

---

> AI researched Convex component ecosystem and monorepo architecture. Created `docs/architecture-research.md`. Key findings: (1) stay pure Convex monorepo — the scale doesn't justify splitting services; (2) 6 components to use: `@convex-dev/agent` (AI assistant backbone with built-in streaming + tool calling), `@convex-dev/rate-limiter`, `@convex-dev/aggregate` (replaces manual denormalization for counts/ratings), `@convex-dev/workpool` (batch embedding + co-occurrence), `@convex-dev/workflow` (durable multi-step pipelines), `convex-helpers` triggers (event-driven side effects); (3) skip `persistent-text-streaming` (agent has built-in delta streaming) and `action-retrier` (workpool is a superset); (4) add `packages/tracking` for client-side behavior hooks, keep recommendation logic inside `packages/backend/convex/model/`; (5) only potential external service: Typesense for search typo tolerance, but only if users actually struggle with Convex's built-in search.
